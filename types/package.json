{
  "name": "@openavatar/types",
  "version": "0.1.0",
  "main": "dist/index",
  "typings": "dist/index",
  "scripts": {
    "build": "yarn tsc",
    "clean": "rm -rf dist/*",
    "coverage": "echo \"@openavatar/types: no coverage yet\"",
    "format": "eslint --ext .ts . --fix",
    "format:check": "eslint --ext .ts . --fix-dry-run",
    "lint": "eslint --ext .ts .",
    "test": "mocha --require ts-node/register --require tsconfig-paths/register --exit --recursive test/**/*.test.ts"
  },
  "devDependencies": {
    "@types/chai": "^4.3.4",
    "@types/mocha": "^10.0.1",
    "@types/node": "^18.15.10",
    "@typescript-eslint/eslint-plugin": "^5.48.0",
    "@typescript-eslint/parser": "^5.48.0",
    "chai": "^4.3.7",
    "eslint": "^8.36.0",
    "mocha": "^10.2.0",
    "typescript": "^5.1.3"
  },
  "author": "Cory Gabrielsen (cory.eth)",
  "license": "GPL-3.0-or-later"
}
